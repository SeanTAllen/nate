Added larger scale examples rather than just the simple ones we have on github wiki

Review design for instantiating with file rather than string with James.

Update Jeweler gemspec configuration to take into account required minimum version for dependencies.

Refactor transform_list in Nate::Engine

Add specs once we are a bit more settled.

Figure out why the hpricot hacks I used to get tests passing ( see attribute check code )

Look for hpricot gotchas

to_html call should have a less ruby specific name
